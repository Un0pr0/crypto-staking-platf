version: '3.8'

services:
  dexfi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dexfi-app
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
